{
  "name": "stugskill",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cp src/*.json dist/ && cp src/*.html dist/ && cp src/*.png dist/ && webpack --watch & web-ext run --source-dir=dist && (cd dist && zip -r ../stugskill.zip .)"
  },
  "keywords": [],
  "author": "codex",
  "license": "BSD-3",
  "type": "commonjs",
  "dependencies": {
    "openskill": "^5.0.1",
    "web-ext": "^10.6.0",
    "webpack": "^5.109.2",
    "webpack-cli": "^7.2.2"
  }
}
